import React from 'react';

const TOTAL_FRAMES = 10;
const TOTAL_ROLLS = 2;

export const Controller = React.createClass({

  getInitialState() {
    return {
      
    };
  },

  render() {
    return (
      
    );
  }
});

export default Controller;